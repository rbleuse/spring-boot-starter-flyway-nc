# Build-time Flyway version. Override per build with -PflywayVersion=<version>
# (used by the CI compatibility matrix). This is the single build-time source of
# truth, read by every module's dependency declarations and by the dependencies BOM.
# It stays explicit because Spring Boot does not manage every Flyway NC module used here.
flywayVersion=12.11.0
